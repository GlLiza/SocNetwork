@model network.BLL.EF.Friendship

<h3>Are you sure you want to delete this?</h3>
<div>
    @using (Html.BeginForm("Delete", "Friendship", new { id = Model.Id }, FormMethod.Post))
    {
        @*@Html.Hidden("id", Model.Id)*@

        <h4>Friendship</h4>
        <hr/>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.User1_id)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.User1_id)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.User2_id)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.User2_id)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Date)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Date)
            </dd>
        </dl>


        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default"/> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
